{% extends 'base.html.twig' %}

{% block title %}API-kortlek{% endblock %}

{% block body %}
<h1>API - Kortlek</h1>

<p>Detta API låter dig interagera med en kortlek:</p>

<ul>
    <li><code><a href="{{ path('api_deck') }}">/api/deck</a></code> – Visar en sorterad kortlek</li>
    <li><code><a href="{{ path('api_deck_shuffle') }}">/api/deck/shuffle</a></code> – Blandar kortleken</li>
    <li><code><a href="{{ path('api_draw_one') }}">/api/deck/draw</a></code> – Drar ett kort från kortleken</li>
    <li><code>/api/deck/draw/{number}</code> – Drar valfritt antal kort (ange antal i URL:en)</li>
    <li><code>/api/deck/deal/{players}/{cards}</code> – Delar ut kort till spelare</li>
</ul>

<p><a href="{{ path('card_index') }}">Tillbaka till kort-menyn</a></p>
{% endblock %}
